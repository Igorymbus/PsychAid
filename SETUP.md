# Пошаговая инструкция по запуску проекта

## ✅ ШАГ 1: Подготовка PostgreSQL

### 1.1. Создать базу данных

Откройте `psql` или pgAdmin и выполните:

```bash
psql -U postgres -f init_db.sql
```

Или вручную:
```sql
CREATE DATABASE "Kursach3" ENCODING 'UTF8';
```

### 1.2. Применить схему БД

```bash
psql -U postgres -d Kursach3 -f schema.sql
```

Это создаст все таблицы, процедуры, функции, триггеры и представления.

---

## ✅ ШАГ 2: Настройка Python окружения

### 2.1. Активировать виртуальное окружение

**PowerShell:**
```powershell
cd c:\Users\User\Desktop\Kursach3
.\venv\Scripts\Activate.ps1
```

**cmd:**
```cmd
cd c:\Users\User\Desktop\Kursach3
venv\Scripts\activate.bat
```

### 2.2. Установить зависимости

```bash
pip install -r requirements.txt
```

---

## ✅ ШАГ 3: Настройка переменных окружения

### PowerShell:
```powershell
$env:POSTGRES_DB="Kursach3"
$env:POSTGRES_USER="postgres"
$env:POSTGRES_PASSWORD="ваш_пароль_от_postgres"
$env:POSTGRES_HOST="127.0.0.1"
$env:POSTGRES_PORT="5432"
```

### cmd:
```cmd
set POSTGRES_DB=Kursach3
set POSTGRES_USER=postgres
set POSTGRES_PASSWORD=ваш_пароль
set POSTGRES_HOST=127.0.0.1
set POSTGRES_PORT=5432
```

**Важно:** Имя БД должно быть `Kursach3` (с заглавной K), как в вашей схеме.

---

## ✅ ШАГ 4: Удалить старые миграции и создать новые

Модели теперь с `managed=False` (таблицы создаются через `schema.sql`), поэтому нужно пересоздать миграции:

```bash
# Удалить старые миграции (кроме __init__.py)
del users\migrations\0001_initial.py
del students\migrations\0001_initial.py
del consultations\migrations\0001_initial.py

# Создать новые миграции (они будут пустыми, т.к. managed=False)
python manage.py makemigrations

# Применить миграции (создаст только служебные таблицы Django)
python manage.py migrate
```

---

## ✅ ШАГ 5: Создать первого администратора

```bash
python manage.py create_admin
```

Введите пароль дважды. По умолчанию логин: `admin`.

---

## ✅ ШАГ 6: Запустить сервер

```bash
python manage.py runserver
```

Откройте http://127.0.0.1:8000/

---

## 🔍 Проверка

После запуска проверьте:

1. **Вход:** http://127.0.0.1:8000/users/login/ — логин `admin`, ваш пароль
2. **Админка Django:** http://127.0.0.1:8000/admin/ — те же учётные данные
3. **Раздел "Пользователи":** должен быть виден в меню (для администратора)
4. **Раздел "Учащиеся":** можно добавить учащегося (нужен класс из `classrooms`)

---

## ⚠️ Важные замечания

1. **Порядок важен:** сначала `schema.sql`, потом `migrate`
2. **Имя БД:** обязательно `Kursach3` (с заглавной K)
3. **Классы:** перед добавлением учащегося создайте хотя бы один класс в таблице `classrooms` (через админку `/admin/students/classroom/` или SQL)
4. **Роли:** роли создаются автоматически в `schema.sql` (admin, psychologist, student)

---

## 🐛 Если что-то не работает

### Ошибка подключения к БД:
- Проверьте, что PostgreSQL запущен
- Проверьте переменные окружения (`$env:POSTGRES_DB` в PowerShell)
- Проверьте пароль пользователя `postgres`

### Ошибка "таблица не существует":
- Убедитесь, что выполнили `schema.sql`
- Проверьте подключение к правильной БД (`Kursach3`)

### Ошибка при создании пользователя:
- Убедитесь, что в таблице `roles` есть запись с `name='admin'` (создаётся в `schema.sql`)

### Ошибка миграций:
- Удалите `db.sqlite3` (если был)
- Удалите папки `__pycache__` в `migrations/`
- Выполните `makemigrations` и `migrate` заново
