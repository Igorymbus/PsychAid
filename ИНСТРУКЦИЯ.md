# 📝 ИНСТРУКЦИЯ ПО ЗАПУСКУ ПРОЕКТА

## ✅ ШАГ 1: Создать базу данных PostgreSQL

Откройте терминал (PowerShell или cmd) и выполните:

```bash
psql -U postgres -f init_db.sql
psql -U postgres -d Kursach3 -f schema.sql
```

**Что это делает:**
- `init_db.sql` — создаёт базу `Kursach3`
- `schema.sql` — создаёт все таблицы, справочники, процедуры, триггеры, представления

---

## ✅ ШАГ 2: Активировать виртуальное окружение

**PowerShell:**
```powershell
cd c:\Users\User\Desktop\Kursach3
.\venv\Scripts\Activate.ps1
```

**cmd:**
```cmd
cd c:\Users\User\Desktop\Kursach3
venv\Scripts\activate.bat
```

---

## ✅ ШАГ 3: Установить зависимости

```bash
pip install -r requirements.txt
```

---

## ✅ ШАГ 4: Настроить переменные окружения

**PowerShell:**
```powershell
$env:POSTGRES_DB="Kursach3"
$env:POSTGRES_USER="postgres"
$env:POSTGRES_PASSWORD="ваш_пароль_от_postgres"
```

**cmd:**
```cmd
set POSTGRES_DB=Kursach3
set POSTGRES_USER=postgres
set POSTGRES_PASSWORD=ваш_пароль
```

⚠️ **Важно:** Имя БД должно быть `Kursach3` (с заглавной K)!

---

## ✅ ШАГ 5: Создать миграции Django

```bash
python manage.py makemigrations
python manage.py migrate
```

**Что это делает:**
- `makemigrations` — создаёт файлы миграций (таблицы уже есть в БД, поэтому миграции будут пустыми)
- `migrate` — создаёт только служебные таблицы Django (сессии, content types и т.д.)

---

## ✅ ШАГ 6: Создать первого администратора

```bash
python manage.py create_admin
```

Введите пароль дважды. По умолчанию логин: `admin`.

---

## ✅ ШАГ 7: Запустить сервер

```bash
python manage.py runserver
```

Откройте в браузере: **http://127.0.0.1:8000/**

---

## 🔐 Вход в систему

- **Логин:** `admin` (или тот, что указали в `create_admin`)
- **Пароль:** тот, что ввели при создании

---

## 📋 Что можно делать после входа

1. **Учащиеся** — добавить учащегося (нужен класс из справочника `classrooms`)
2. **Обращения** — зарегистрировать обращение за консультацией
3. **Журнал консультаций** — добавить запись о консультации
4. **Отчёты** — посмотреть статистику
5. **Пользователи** (только для администратора) — управление пользователями и ролями

---

## ⚠️ Важные замечания

1. **Порядок важен:** сначала `schema.sql`, потом `migrate`
2. **Классы:** перед добавлением учащегося создайте хотя бы один класс:
   - Через админку: http://127.0.0.1:8000/admin/students/classroom/
   - Или SQL: `INSERT INTO classrooms (name) VALUES ('10А');`
3. **Роли:** автоматически создаются в `schema.sql`:
   - `admin` — Администратор
   - `psychologist` — Школьный психолог
   - `student` — Учащийся

---

## 🐛 Решение проблем

### Ошибка подключения к БД
- Проверьте, что PostgreSQL запущен
- Проверьте переменные окружения: `$env:POSTGRES_DB` в PowerShell
- Проверьте пароль пользователя `postgres`

### Ошибка "таблица не существует"
- Убедитесь, что выполнили `schema.sql`
- Проверьте подключение к правильной БД (`Kursach3`)

### Ошибка при создании пользователя
- Убедитесь, что в таблице `roles` есть запись с `name='admin'`
- Проверьте: `SELECT * FROM roles;` в psql

### Ошибка миграций
- Удалите `db.sqlite3` (если был)
- Удалите папки `__pycache__` в `migrations/`
- Выполните `makemigrations` и `migrate` заново

---

## 📁 Структура БД

- **Справочники:** `roles`, `request_statuses`, `consultation_forms`
- **Основные:** `users`, `students`, `classrooms`, `teachers`, `parents`
- **Рабочие:** `requests`, `consultations`, `notes`, `attachments`
- **Служебные:** `events`, `reports`, `logs`

Все таблицы создаются через `schema.sql`, Django их не изменяет (`managed=False`).
